<!-- TUNE (UPDATE) ACTIVITY PARTIAL -->

<div class="alert alert-info" role="alert">
  <i class="fa fa-lg fa-edit activity-icon"></i>
  <%= link_to activity.owner.username, activity.owner if activity.owner %> edited  
  <% if activity.trackable %>
    the tune <%= link_to activity.trackable.name, activity.trackable %>
  <% else %>
    a tune which has since been removed
  <% end %>
</div>