<!-- REPERTOIRE (CREATE) ACTIVITY PARTIAL -->

<div class="alert alert-warning" role="alert">
  <i class="fa fa-lg fa-user-plus activity-icon"></i>
  <%= link_to activity.owner.username, activity.owner if activity.owner %> added 
  <% if activity.trackable %>
    <%= link_to Tune.find(activity.trackable.tune_id).name, tune_path(Tune.find(activity.trackable.tune_id)) %> to repetoire
  <% else %>
    a tune to repetoire which has since been removed
  <% end %>
</div>