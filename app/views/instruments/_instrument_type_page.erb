<% instrument_types.each do |instrument_type| %>
  <%= link_to image_tag(instrument_type + '.svg', :class => 'svg-xxxs svg-padding-right-sm') +
      " #{instrument_type.pluralize}", eval("#{instrument_type.pluralize.downcase}_path") %>
<% end %>
